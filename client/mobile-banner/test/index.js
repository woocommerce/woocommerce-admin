describe( 'MobileAppBanner', () => {
	it( 'closes if the user dismisses it', () => {} );

	it( 'goes to the playstore link if the user clicks install', () => {} );

	it( 'records an install event for install', () => {} );

	it( 'records a dismiss event for dismiss', () => {} );
} );
